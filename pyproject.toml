[project]
name = "blockchain-based-automated-fire-insurance-claim-system"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Priyankush231", email = "biswaspriyankush7@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
]

[project.scripts]
blockchain-based-automated-fire-insurance-claim-system = "blockchain_based_automated_fire_insurance_claim_system:main"

[build-system]
requires = ["uv_build>=0.12.19,<0.13.0"]
build-backend = "uv_build"
